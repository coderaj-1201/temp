# ── Retrieval Pipeline .env ───────────────────────────────────────────────────
# Copy to container-code/.env for local dev. Never commit .env.
# In ACA these are set as environment variables by the Bicep deployment.

# ── Azure AI Foundry ──────────────────────────────────────────────────────────
# Portal: AI Foundry → your project → Overview → "Project endpoint"
AZURE_FOUNDRY_PROJECT_ENDPOINT=https://<hub>.services.ai.azure.com/api/projects/<project>
AZURE_OPENAI_CHAT_DEPLOYMENT=gpt-4o
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-ada-002
AZURE_OPENAI_API_VERSION=2024-08-01-preview
AZURE_OPENAI_EVAL_DEPLOYMENT=gpt-4o-mini

# ── Azure AI Search ───────────────────────────────────────────────────────────
# Portal: AI Search resource → Overview → Url
AZURE_SEARCH_ENDPOINT=https://<your-search>.search.windows.net
# Portal: AI Search → Settings → Keys → Primary admin key (LOCAL DEV only)
# In ACA: keyless via Managed Identity — leave blank or omit
AZURE_SEARCH_API_KEY=
AZURE_SEARCH_INDEX=idx-rag
AZURE_SEARCH_SEMANTIC_CONFIG=rag-semantic-config

# ── Azure Service Bus ─────────────────────────────────────────────────────────
# Portal: Service Bus namespace → Overview → Host name (e.g. <ns>.servicebus.windows.net)
AZURE_SERVICE_BUS_NAMESPACE=<namespace>.servicebus.windows.net
# IMPORTANT: rag-inbound and rag-outbound queues MUST have Sessions enabled

# ── Azure Cosmos DB ───────────────────────────────────────────────────────────
# Portal: Cosmos DB account → Overview → URI
AZURE_COSMOS_ENDPOINT=https://<account>.documents.azure.com:443/
AZURE_COSMOS_DATABASE=rag-analytics
AZURE_COSMOS_CONTAINER_EVALUATIONS=evaluations
AZURE_COSMOS_CONTAINER_FEEDBACK=feedback

# ── Entra ID (for JWT token validation) ───────────────────────────────────────
# Portal: Entra ID → Overview → Tenant ID
AZURE_TENANT_ID=<tenant-id>
# Portal: Entra ID → App registrations → your app → Application (client) ID
AZURE_CLIENT_ID=<app-client-id>

# ── RAG tuning ────────────────────────────────────────────────────────────────
CONFIDENCE_THRESHOLD=0.75
MAX_RETRIEVAL_ATTEMPTS=3
RETRIEVAL_TOP_K=5
SYNTHESIS_TEMPERATURE=0.0

# ── Observability ─────────────────────────────────────────────────────────────
# Portal: App Insights → Overview → Connection String
APPLICATIONINSIGHTS_CONNECTION_STRING=
LOG_LEVEL=INFO
